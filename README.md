# Zeb's Personal Automation Helpers

## scripts

The `scripts` folder contains any programs or functions that are run repeatedly.

## config

The `config` folder contains any configuration information utilized repeatedly.